@using RegisterLogin.Domain.ViewModels
@model LoginViewModel

@section script {
    <script src="~/js/site.js"></script>
}

@{
    ViewData["Title"] = "ورود";
}



<main class="client-page" id="yy">
    <div class="login-container" id="logincontainer">
        <header><h3> ورود به سایت </h3></header>
        <div style="text-align:center">
            <form method="post" id="loginForm">
                <div class="form-group">
                    <input asp-for="Email" class="form-control" placeholder="ایمیل" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input asp-for="Password" class="form-control" placeholder="رمز عبور" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div class="form-check">
                    <label><input asp-for="RememberMe" type="checkbox" name="">  مرا بخاطر بسپار </label>
                </div>
                <div>
                    <button type="button" class="btn-login" onclick="loginUserPost()"> ورود به سایت </button>
                </div>
            </form>
            <div class="footer">
                <p>حساب کاربری ندارید؟ <a type="button" onclick="registerUser()">ثبت‌نام کنید</a></p>
                @* <p>حساب کاربری ندارید؟ <a href="/Login/Register">ثبت‌نام کنید</a></p> *@
                <p><a type="button" onclick="forgotPass()">بازیابی رمز عبور</a></p>
            </div>
        </div>
    </div>
</main>



<div id="Container" style="display:none;">
    <!--formha inja render mishan-->
    <div id="errorMessage" style="color: red;"></div>
</div>

<div id="errorMessage" style="color: red;"></div>

<style>
    body {
        background: linear-gradient(to right, #006055, #96c93d);
    }

    .navbar {
        background: linear-gradient(to right, #006055, #96c93d);
    }

    .login-container {
        background-color: white;
        border-radius: 15px;
        padding: 40px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        width: 350px;
        text-align: center;
        animation: slide-up 0.6s ease-in-out;
    }

    .btn-login {
        background-color: #96c93d;
        color: white;
        padding: 12px 0;
        border: none;
        border-radius: 10px;
        width: 100%;
        font-size: 18px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

        .btn-login:hover {
            background-color: #006055;
        }

    .footer {
        margin-top: 20px;
        font-size: 14px;
        color: #000;
    }

        .footer a {
            color: #000;
            text-decoration: none;
        }

            .footer a:hover {
                text-decoration: underline;
            }

    .form-control {
        width: 100%;
        padding: 12px;
        margin: 10px 0;
        border-radius: 10px;
        border: 1px solid #ddd;
        box-sizing: border-box;
        font-size: 16px;
    }

        .form-control:focus {
            outline: none;
            border-color: #96c93d;
            box-shadow: 0 0 10px rgba(150, 201, 61, 0.5);
        }
</style>